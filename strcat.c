#include <stdio.h>
#include <string.h>

int main(){
    char str1[] = "Hi! ";
    char str2[] = "I'm Adib";
    puts(strcat(str1,str2));// strcat(target,source);
    // puts(str);
    return 0;
}